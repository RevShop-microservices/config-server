# MySQL Database Connection details
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3307/EcommerceDB}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Security JWT properties
jwt.secret=${JWT_SECRET:myVeryStrongSecretKeyForJwtAuthentication123456789}
jwt.expiration=3600000

# RabbitMQ configuration for asynchronous consumer
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${SPRING_RABBITMQ_PORT:5672}
spring.rabbitmq.username=${SPRING_RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${SPRING_RABBITMQ_PASSWORD:guest}

# Mail configuration
spring.mail.host=smtp.office365.com
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:bl.en.u4aie22063@bl.students.amrita.edu}
spring.mail.password=${MAIL_PASSWORD:uMureeoy}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.from=${MAIL_FROM:bl.en.u4aie22063@bl.students.amrita.edu}

logging.level.com.example.user_service=DEBUG
